// Full review UI is built in P5. This is a runtime-safe placeholder so
// the lazy route resolves while the rest of the import pipeline is wired up.
export { FrameworkImportReviewPage } from "./FrameworkImportReviewPage.impl";
